<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Settings - Ecoinfinite Nexus</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <style>
        :root {
            --primary: #28821c;
            --secondary: #3f37c9;
            --accent: #4895ef;
            --danger: #f72585;
            --success: #4cc9f0;
            --warning: #f8961e;
            --light: #f8f9fa;
            --dark: #212529;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            display: flex;
            background-color: #f5f7fa;
        }

        .sidebar {
            width: 250px;
            background: white;
            height: 100vh;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            position: fixed;
            transition: all 0.3s;
        }

        .sidebar-header {
            padding: 20px;
            background: var(--primary);
            color: white;
            display: flex;
            align-items: center;
        }

        .sidebar-header h3 {
            margin-left: 10px;
        }

        .sidebar-menu {
            padding: 20px 0;
        }

        .sidebar-menu li {
            list-style: none;
            margin-bottom: 5px;
        }

        .sidebar-menu a {
            display: flex;
            align-items: center;
            padding: 12px 20px;
            color: var(--dark);
            text-decoration: none;
            transition: all 0.3s;
        }

        .sidebar-menu a:hover, .sidebar-menu a.active {
            background: #e9ecef;
            border-left: 4px solid var(--primary);
        }

        .sidebar-menu i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
        }

        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: 20px;
            transition: all 0.3s;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
            background: white;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            border-radius: 5px;
            margin-bottom: 20px;
        }

        .user-info {
            display: flex;
            align-items: center;
        }

        .user-info img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            margin-right: 10px;
        }

        .user-dropdown {
            position: relative;
            cursor: pointer;
        }

        .user-dropdown span {
            display: flex;
            align-items: center;
            gap: 5px;
            color: var(--dark);
        }

        .dropdown-menu {
            display: none;
            position: absolute;
            top: 40px;
            right: 0;
            background: white;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            z-index: 100;
            min-width: 150px;
        }

        .dropdown-menu a {
            display: block;
            padding: 10px 15px;
            color: var(--dark);
            text-decoration: none;
        }

        .dropdown-menu a:hover {
            background: #f1f1f1;
        }

        .card {
            background: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            max-width: 600px;
            margin: auto;
        }

        .card-header h3 {
            font-size: 18px;
            margin-bottom: 10px;
        }

        .bg-primary {
            background: var(--primary);
            color: white;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            background-color: var(--primary);
            color: white;
            cursor: pointer;
        }

        @media (max-width: 992px) {
            .sidebar {
                width: 80px;
            }

            .sidebar-header h3, .sidebar-menu span {
                display: none;
            }

            .sidebar-menu a {
                justify-content: center;
                padding: 15px 0;
            }

            .main-content {
                margin-left: 80px;
            }
        }

        /* Update success message styles */
        #updateMessage {
            display: none;
            background-color: #d4edda;
            color: #155724;
            padding: 10px 15px;
            margin-bottom: 20px;
            border: 1px solid #c3e6cb;
            border-radius: 4px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
            text-align: center;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="sidebar-header">
            <i class="fas fa-home fa-2x"></i>
            <h3>Ecoinfinite Nexus</h3>
        </div>
        <ul class="sidebar-menu">
            <li><a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i><span>Dashboard</span></a></li>
            <li><a href="/admin/customers"><i class="fas fa-users"></i><span>Customers Deails</span></a></li>
            <li><a href="/admin/waste"><i class="fas fa-trash"></i><span>Waste Collection</span></a></li>
            <!-- <li><a href="/admin/handicrafts"><i class="fas fa-concierge-bell"></i><span>Handicrafts</span></a></li> -->
            <li><a href="/admin/payments"><i class="fas fa-calendar-check"></i><span>Payments Details</span></a></li>
            <li><a href="/admin/settings" class="active"><i class="fas fa-cog"></i><span>Settings</span></a></li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <div class="header">
            <h2>Settings</h2>
            <div class="user-info">
                <img src="https://t3.ftcdn.net/jpg/00/07/32/48/360_F_7324855_mx4CEBWTr81XLOrlQccCROtP2uNR7xbk.jpg" alt="Admin" />
                <div class="user-dropdown" id="userDropdown">
                    <span id="dropdownToggle">Admin <i class="fas fa-caret-down"></i></span>
                    <div class="dropdown-menu" id="dropdownMenu">
                        <a href="/admin/profile"><i class="fas fa-user"></i> Profile</a>
                        <a href="/admin/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Success Message -->
        <div id="updateMessage">Updated successfully!</div>

        <!-- Settings Form -->
        <div class="card">
            <div class="card-header">
                <h3>Update Admin Settings</h3>
            </div>
            <div class="card-body">
                <form action="/admin/settings" method="POST">
                    <div style="margin-bottom: 15px;">
                        <label for="username">Username</label><br />
                        <input type="text" name="username" id="username" value="<%= admin.username %>" required />
                    </div>

                    <div style="margin-bottom: 15px;">
                        <label for="email">Email</label><br />
                        <input type="email" name="email" id="email" value="<%= admin.email %>" required />
                    </div>

                    <div style="margin-bottom: 15px;">
                        <label for="password">New Password</label><br />
                        <input
                          type="password"
                          name="password"
                          id="password"
                          placeholder="Enter new password"
                        />
                    </div>

                    <button type="submit">Update</button>
                </form>
            </div>
        </div>
    </div>

    <script>
        const dropdownToggle = document.getElementById('dropdownToggle');
        const dropdownMenu = document.getElementById('dropdownMenu');

        dropdownToggle.addEventListener('click', () => {
            dropdownMenu.style.display = dropdownMenu.style.display === 'block' ? 'none' : 'block';
        });

        document.addEventListener('click', (e) => {
            if (!document.getElementById('userDropdown').contains(e.target)) {
                dropdownMenu.style.display = 'none';
            }
        });

        // Show updated message if URL has updated=true
        const params = new URLSearchParams(window.location.search);
        if (params.get('updated') === 'true') {
            const msg = document.getElementById('updateMessage');
            if (msg) {
                msg.style.display = 'block';

                // Hide message after 3 seconds and clean URL
                setTimeout(() => {
                    msg.style.display = 'none';

                    // Remove updated param from URL without reloading page
                    const url = new URL(window.location);
                    url.searchParams.delete('updated');
                    window.history.replaceState({}, document.title, url.toString());
                }, 3000);
            }
        }
    </script>
</body>
</html>
